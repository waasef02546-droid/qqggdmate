# 本次会话改进记录

## 你的指示清晰度

整体清晰，尤其是你不断把问题从“代理重签名身份认证”推进到“基于 SAGA 的数据共享扩展”。这个转向是关键，使选题从较弱的身份认证重复工作，变成更有辨识度的 PRE-SAGA 数据隐私扩展。

最清晰的部分：

- 明确指出 SAGA 本身是优秀会议文章标杆。
- 明确指出短小 toy code 不足以支撑论文。
- 明确要求时间线、任务清单、代码框架、测试攻击方向。
- 明确要求实验跟踪文档和后续追加日志。
- 明确限制 SAGA 原文二次审阅最多两遍，避免重复消耗。

## 可以优化的地方

1. 可以更早明确最终投稿目标。

如果目标是本科论文、中文会议、工作坊，代码和形式化分析要求不同；如果目标是高水平安全会议，则必须有更严格的 baseline、攻击模型和形式化验证。

2. 可以区分“现在要执行”和“现在只做设计”。

这次你说“可以先进行设计”，我按设计文档处理。后续如果需要开始搭代码框架，可以直接说“开始实现阶段 1”。

3. 可以明确是否允许拉取 SAGA 官方仓库。

SAGA 仓库是公开的，但本地拉取会涉及网络和较多依赖。后续最好明确是否要复现原仓库，还是只参考其结构做独立实现。

4. 可以提前说明论文语言。

目前文档以中文为主。如果后续准备英文投稿，需要尽早切换标题、摘要、贡献和实验命名。

## 我这边的不足

1. 前期对 SAGA 源码状态判断不够主动。

在你提出“SAGA 源码可能没给出”之前，我没有主动指出论文第 2 页已经给出 GitHub 仓库链接。这一点应更早核查。

2. 初版原型过于 toy。

它适合快速验证机制，但确实不足以支撑优秀会议文章。后续应尽快拆分为模块化框架，并补充真实 baseline 和攻击测试。

3. 文件命名曾保留旧主题。

之前 `prs_chainauth_draft.md` 和 `prs_chainauth.py` 残留旧命名，后续已改为 `pre_saga_draft.md` 和 `pre_saga.py`。以后主题切换时应同步检查文件名、README 和引用。

## 后续建议

下一步最合适的任务是：

> 按 `paper/publication_plan.md` 的阶段 0，先完成 SAGA baseline gap analysis，并决定是否拉取官方仓库做最小复现。

这一步完成后，再进入模块化实现，避免在没有 baseline 边界的情况下盲目写代码。

