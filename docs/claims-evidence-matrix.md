# Claims-evidence matrix

This matrix prevents paper wording, implementation behavior, and evidence from drifting apart.
Statuses are intentionally conservative until a bounded work package re-audits each row.

| ID | Bounded claim | Implementation path | Current evidence | Status | Limitation / next action |
|---|---|---|---|---|---|
| C-001 | Contact authorization and data authorization are distinct gates. | `project/presaga/provider/saga_adapter.py`, `app.py`, `data_policy.py`, `token_service.py` | `test_contact_data_binding.py`; agent, HTTP, and SAGA bridge integration tests | verified | Contact authorization remains a prerequisite and does not replace the separate data-policy decision. |
| C-002 | Allowed requests can receive policy-bound data access while mismatched requester, purpose, class, record, or version is denied through the same authoritative Provider service path. | `project/presaga/provider/app.py`, `server.py`, `token_service.py`; policy-aware stores; `project/experiments/provider_harness.py` | Accepted CRYPTO-001 manifest: 8/8 blocking attacks and 4/4 tasks; focused checks and 101-test live-Mongo regression | verified | This verifies prototype authorization behavior, not resistance after a legitimate requester has decrypted data. |
| C-003 | In the bounded data-plane path, the Provider transforms an Umbral-encrypted DEK with a signed and verified 1-of-1 KFrag without receiving either private key or plaintext; the exposed-state/public-API recovery regression finds no DEK while the intended requester decrypts. Owner rotation now uses a separate authenticated owner/KMS rewrap artifact rather than passing the owner private key to the Provider. | `project/presaga/crypto/umbral_pre.py`, `crypto/key_custody.py`, `provider/pre_proxy.py`, encrypted stores, authoritative experiments | Accepted CRYPTO-001 evidence plus KEYCUSTODY-001 custody boundary, tamper/replay, leakage, and release-gate evidence | prototype-bounded | This is empirical adapter/process-boundary evidence over `nucypher-core==0.15.0`, not cryptanalysis, memory forensics, an independent audit, HSM protection, or a production claim. KFrags remain owner/requester key-pair scoped; Provider/requester collusion across same-owner capsules and side channels are outside the established boundary. |
| C-004 | The current attack, task, performance, formal, SAGA-bridge, custody, Mongo, claim-matrix, traceability, and authoritative-paper inputs are bound to one source/configuration state and are independently integrity-checkable against the checked-in manifest. | `project/experiments/release.py`, `release_verifier.py`, `project/scripts/verify_release.py`, `project/results/release-manifest.json` | KEYCUSTODY-001 clean-input manifest; independent verifier pass; 24 artifact hashes and 10 semantic gates | prototype-bounded | The manifest is not signed or externally witnessed. This proves manifest-relative integrity and consistency, not scientific generality, clean-room reproduction on another host, or equivalence to SAGA's full evaluation. |
| C-005 | The single-process Provider signs a DataToken to one exact Contact session, revalidates it at PRE consumption, and authorizes at most one concurrent consumption of a one-use token. | `project/presaga/protocol/schemas.py`, `provider/token_service.py`, `provider/app.py`, `provider/pre_proxy.py`, `provider/server.py` | CORE-001 focused/full regression plus CORE-002 focused/full regression | verified | No Contact revocation or distributed multi-Provider atomicity is established. |
| C-006 | Active server-managed AID registrations determine the key fingerprints, algorithms, and versions used at issuance and consumption. For rotation, the Provider reconstructs the custody request from the active source registration and prepared target registration, and verifies the artifact only with that authoritative source public key. | `project/presaga/provider/registry.py`, `app.py`, `token_service.py`, `pre_proxy.py`, `crypto/key_custody.py`, persistence adapters | registration/substitution tests; custody wrong-key, forged-artifact, cross-request, and target-context denials | prototype-bounded | Static Bearer management authentication is not production administrator identity. The custody signature proves possession for this operation, but the prototype reuses the Umbral source key rather than a separately registered KMS attestation key. |
| C-007 | In the bounded single-provider prototype, owner-wrapped DEKs carry authoritative provenance; prepare/export/owner-custody/stage/commit or abort persists across JSON/Mongo restart. The Provider does not accept the source private key or plaintext DEK, and stages only a signed artifact bound to the exact rotation, record, revision, registrations, contexts, source wrapper, and target wrapper. Exact artifact replay is idempotent and conflicting replay fails closed. | encrypted stores; registry/app/server; `crypto/key_custody.py`; owner-side artifact CLI; persistence adapters | focused custody/rotation/recovery tests; live Mongo CAS/restart tests; semantic release gate; ADR 0009 | prototype-bounded | The owner/KMS process still materializes the source private key and DEK. No HSM, remote attestation, secure-memory guarantee, multi-object transaction, cryptographic erasure of copied KFrags, or multi-process linearizability is claimed. |
| C-008 | In the bounded single-active-Provider prototype, JSON and Mongo implement one revisioned aggregate persistence contract; Mongo-backed HTTP operations persist state, restart restores signed custody-stage state against encrypted-object documents, and an aggregate CAS loser is fenced. Publication-facing objects use versioned Umbral envelopes and legacy private-key rotation input fails closed. | repositories, server, Mongo store, `crypto/key_custody.py`, Mongo E2E | repository/Mongo Provider tests; live custody rotation CAS/recovery; concrete format rejection; KEYCUSTODY-001 release gate | prototype-bounded | One active writer is assumed. CAS is not leader election; aggregate metadata and encrypted objects remain separate atomicity domains. Older stored wraps remain readable, but an in-progress legacy rotation must be prepared again; automatic ciphertext migration and production deployment security are not established. |

## Status vocabulary

- `verified`: implementation, repeatable evidence, and bounded wording agree.
- `provisional`: supporting artifacts exist but need work-package-level freshness or path audit.
- `partial`: only part of the stated property is evidenced.
- `prototype-bounded`: implemented evidence exists but does not establish production security.
- `unsupported`: current evidence cannot justify the wording.
- `blocked`: verification cannot proceed until a named dependency or environment issue is resolved.
