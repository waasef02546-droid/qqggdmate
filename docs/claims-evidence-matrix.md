# Claims-evidence matrix

This matrix prevents paper wording, implementation behavior, and evidence from drifting apart.
Statuses are intentionally conservative until a bounded work package re-audits each row.

| ID | Bounded claim | Implementation path | Current evidence | Status | Limitation / next action |
|---|---|---|---|---|---|
| C-001 | Contact authorization and data authorization are distinct gates. | `project/presaga/provider/saga_adapter.py`, `app.py`, `data_policy.py`, `token_service.py` | `test_contact_data_binding.py`; agent, HTTP, and SAGA bridge integration tests | verified | Contact authorization remains a prerequisite and does not replace the separate data-policy decision. |
| C-002 | Allowed requests can receive policy-bound data access while mismatched requester, purpose, class, record, or version is denied through the same authoritative Provider service path. | `project/presaga/provider/app.py`, `server.py`, `token_service.py`; policy-aware stores; `project/experiments/provider_harness.py` | Accepted REL-001 manifest; 7/7 blocking attacks; 4/4 tasks; focused and 89-test full regression | verified | This verifies prototype authorization behavior, not production cryptography or resistance after a legitimate requester has decrypted data. |
| C-003 | The Provider service control flow transforms an encrypted DEK without explicitly receiving a plaintext DEK or record; however, the concrete ToyPRE backend does **not** provide Provider-side DEK confidentiality because its public material is sufficient to recover the DEK. | `project/presaga/crypto/toy_pre.py`, encrypted storage, re-encryption path | Active `provider_plaintext_probe`; AES-envelope tests; symbolic ProVerif outputs; accepted REL-001 manifest | partial | Structural non-materialization is observed. Concrete cryptographic Provider confidentiality is unsupported and must not be claimed until a reviewed PRE/HPKE backend defeats the active recovery probe. |
| C-004 | The current attack, task, performance, formal, SAGA-bridge, Mongo, claim-matrix, traceability, and authoritative-paper inputs are bound to one source/configuration state and are independently integrity-checkable against the checked-in manifest. | `project/experiments/release.py`, `release_verifier.py`, `project/scripts/verify_release.py`, `project/results/release-manifest.json` | Accepted REL-001 manifest; independent verifier pass; artifact/source/config hashes; 89-test full regression | prototype-bounded | The manifest is not signed or externally witnessed. This proves manifest-relative integrity and consistency, not scientific generality, clean-room reproduction on another host, or equivalence to SAGA's full evaluation. |
| C-005 | The single-process Provider signs a DataToken to one exact Contact session, revalidates it at PRE consumption, and authorizes at most one concurrent consumption of a one-use token. | `project/presaga/protocol/schemas.py`, `provider/token_service.py`, `provider/app.py`, `provider/pre_proxy.py`, `provider/server.py` | CORE-001 focused/full regression plus CORE-002 focused/full regression | verified | No Contact revocation or distributed multi-Provider atomicity is established. |
| C-006 | In the requester path, an active server-managed AID registration determines the signed key fingerprint and registration version at token issuance; consumption re-resolves the same active version and fails closed after replacement, revocation, unknown state, caller-key mismatch, or unauthenticated legacy restore. | `project/presaga/provider/registry.py`, `app.py`, `token_service.py`, `server.py`, `json_repository.py`, `mongo_repository.py` | `test_agent_registry.py`, `test_management_data_plane.py`, `test_provider_http_service.py`; ADR 0003; CORE-002 focused and isolated full regression | verified | The in-process capability and static Bearer token are prototype management authentication. Mongo runtime CAS, owner-key rewrap, distributed transactions, and cryptographic rekey-target proof are not established. |
| C-007 | In the bounded single-provider prototype, owner-wrapped DEKs carry authoritative registration provenance; an authenticated prepare/stage/commit or abort lifecycle persists its immutable journal and encrypted-object state across JSON restart, validates reachable recovery states, and conditionally cleans only the exact rotation's staged or retired wraps. MongoDB uses record-revision and journal-state compare-and-swap filters verified against a live local service. | `project/presaga/storage/encrypted_store.py`, `mongo_encrypted_store.py`, `provider/registry.py`, `app.py`, `server.py`, `json_repository.py`, `mongo_repository.py`, `crypto/key_rotation.py` | `test_encrypted_store_provenance.py`, `test_owner_key_rotation.py`, `test_rotation_recovery.py`, `test_mongodb_rotation_cas.py`; ADR 0004 and 0005; CORE-003 regression and CORE-004 focused/live-Mongo evidence | prototype-bounded | JSON replacement is single-process and Mongo guarantees are per document; no atomic registration/journal/multi-object transaction, multi-process linearizability, HSM/KMS integration, or production PRE/HPKE security is claimed. |
| C-008 | In the bounded single-active-Provider prototype, JSON and Mongo implement one revisioned aggregate persistence contract; Mongo-backed HTTP operations automatically persist state, restart restores an in-progress owner rotation against existing encrypted-object documents, and an aggregate CAS loser is fenced from later service operations. | `project/presaga/provider/repository.py`, `json_repository.py`, `mongo_repository.py`, `server.py`, `storage/mongo_encrypted_store.py`, `experiments/e2e/mongodb_e2e.py` | `test_provider_repository.py`, `test_mongodb_provider_service.py`, migrated `test_mongodb_e2e.py`; ADR 0006; CORE-005 focused and full live-Mongo regression | prototype-bounded | One active writer is assumed. CAS is not leader election; aggregate metadata and encrypted objects are separate atomicity domains; Mongo document-size limits, static Bearer auth, and toy PRE/HPKE remain non-production boundaries. |

## Status vocabulary

- `verified`: implementation, repeatable evidence, and bounded wording agree.
- `provisional`: supporting artifacts exist but need work-package-level freshness or path audit.
- `partial`: only part of the stated property is evidenced.
- `prototype-bounded`: implemented evidence exists but does not establish production security.
- `unsupported`: current evidence cannot justify the wording.
- `blocked`: verification cannot proceed until a named dependency or environment issue is resolved.
