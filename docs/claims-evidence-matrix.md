# Claims-evidence matrix

This matrix prevents paper wording, implementation behavior, and evidence from drifting apart.
Statuses are intentionally conservative until a bounded work package re-audits each row.

| ID | Bounded claim | Implementation path | Current evidence | Status | Limitation / next action |
|---|---|---|---|---|---|
| C-001 | Contact authorization and data authorization are distinct gates. | `project/presaga/provider/saga_adapter.py`, `app.py`, `data_policy.py`, `token_service.py` | `test_contact_data_binding.py`; agent, HTTP, and SAGA bridge integration tests | verified | Contact authorization remains a prerequisite and does not replace the separate data-policy decision. |
| C-002 | Allowed requests can receive policy-bound data access while mismatched requester, purpose, class, record, or version is denied through the same authoritative Provider service path. | `project/presaga/provider/app.py`, `server.py`, `token_service.py`; policy-aware stores; `project/experiments/provider_harness.py` | Accepted CRYPTO-001 manifest: 8/8 blocking attacks and 4/4 tasks; focused checks and 101-test live-Mongo regression | verified | This verifies prototype authorization behavior, not resistance after a legitimate requester has decrypted data. |
| C-003 | In the bounded data-plane path, the Provider transforms an Umbral-encrypted DEK with a signed and verified 1-of-1 KFrag without receiving either private key or plaintext; the active public-material recovery probe fails while the intended requester decrypts. | `project/presaga/crypto/umbral_pre.py`, `provider/pre_proxy.py`, encrypted stores, authoritative experiments | Accepted clean-input CRYPTO-001 release; Umbral positive/tamper/context/key tests; active recovery probe; abstract ProVerif outputs | prototype-bounded | This is empirical adapter evidence over `nucypher-core==0.15.0`, not an independent cryptographic audit or production claim. KFrags are owner/requester key-pair scoped; Provider/requester collusion across same-owner capsules, side channels, and compromise of the trusted management plane are outside the established boundary. |
| C-004 | The current attack, task, performance, formal, SAGA-bridge, Mongo, claim-matrix, traceability, and authoritative-paper inputs are bound to one source/configuration state and are independently integrity-checkable against the checked-in manifest. | `project/experiments/release.py`, `release_verifier.py`, `project/scripts/verify_release.py`, `project/results/release-manifest.json` | Accepted clean-input CRYPTO-001 manifest; independent verifier pass; 23 artifact hashes | prototype-bounded | The manifest is not signed or externally witnessed. This proves manifest-relative integrity and consistency, not scientific generality, clean-room reproduction on another host, or equivalence to SAGA's full evaluation. |
| C-005 | The single-process Provider signs a DataToken to one exact Contact session, revalidates it at PRE consumption, and authorizes at most one concurrent consumption of a one-use token. | `project/presaga/protocol/schemas.py`, `provider/token_service.py`, `provider/app.py`, `provider/pre_proxy.py`, `provider/server.py` | CORE-001 focused/full regression plus CORE-002 focused/full regression | verified | No Contact revocation or distributed multi-Provider atomicity is established. |
| C-006 | In the requester path, an active server-managed AID registration determines the signed key fingerprint, backend algorithm, and registration version at token issuance; consumption re-resolves the same active version, and the Umbral transform cryptographically verifies the KFrag receiving key against that trusted registration. | `project/presaga/provider/registry.py`, `app.py`, `token_service.py`, `pre_proxy.py`, `crypto/umbral_pre.py`, persistence adapters | registry/management/HTTP tests; Umbral wrong-requester and KFrag verification tests; CRYPTO-001 focused checks | prototype-bounded | Static Bearer management authentication and public-key registration do not prove live private-key possession. Revocation cannot erase adversarially copied KFrags, and no distributed transaction is established. |
| C-007 | In the bounded single-provider prototype, owner-wrapped DEKs carry authoritative registration and backend-algorithm provenance; prepare/stage/commit or abort persists its journal and object state across JSON restart, and Mongo uses tested per-document conditional updates. Concrete Umbral rotation creates a fresh target MessageKit. | encrypted stores; registry/app/server; persistence adapters; `crypto/key_rotation.py`; `crypto/umbral_pre.py` | rotation, recovery, Mongo CAS, and concrete-backend rewrap tests; ADR 0004, 0005, and 0008 | prototype-bounded | Rotation is trusted-management decrypt-and-fresh-encrypt and can materialize the source private key and DEK inside that boundary. No owner-side KMS/HSM isolation, multi-object transaction, cryptographic erasure of copied KFrags, or multi-process linearizability is claimed. |
| C-008 | In the bounded single-active-Provider prototype, JSON and Mongo implement one revisioned aggregate persistence contract; Mongo-backed HTTP operations persist state, restart restores in-progress rotation against encrypted-object documents, and an aggregate CAS loser is fenced. Publication-facing objects use versioned Umbral envelopes and legacy toy/stub bytes fail closed under the concrete backend. | repositories, server, Mongo store, `crypto/umbral_pre.py`, Mongo E2E | repository/Mongo Provider tests; concrete format rejection tests; accepted CRYPTO-001 live-Mongo gate | prototype-bounded | One active writer is assumed. CAS is not leader election; aggregate metadata and encrypted objects remain separate atomicity domains. Automatic legacy ciphertext migration and production deployment security are not established. |

## Status vocabulary

- `verified`: implementation, repeatable evidence, and bounded wording agree.
- `provisional`: supporting artifacts exist but need work-package-level freshness or path audit.
- `partial`: only part of the stated property is evidenced.
- `prototype-bounded`: implemented evidence exists but does not establish production security.
- `unsupported`: current evidence cannot justify the wording.
- `blocked`: verification cannot proceed until a named dependency or environment issue is resolved.
